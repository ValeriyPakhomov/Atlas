.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.env
.env.*
!.env.example
node_modules/
.next/

# Vercel CLI: project linkage (orgId/projectId) and the generated .env.local,
# which carries a short-lived OIDC token. `.env` and `.env.*` above already
# cover .env.local; this entry covers the CLI's own directory.
.vercel/
*.log
